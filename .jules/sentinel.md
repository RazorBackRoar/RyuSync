# Sentinel Security Agent Guidelines

- **No Redundant Contact / Security Updates**: Before opening a PR for `SECURITY.md`, contact emails, or security policies, verify if an equivalent change was already merged or closed.
- **Single Source of Truth**: Do not generate duplicate PRs for previously resolved security notifications.
- **Branch Naming**: Always use the prefix `sentinel-` for automated security PRs.

